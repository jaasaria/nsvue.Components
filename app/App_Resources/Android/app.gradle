// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "org.nativescript.jaacodeblock"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
